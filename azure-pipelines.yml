# Starter pipeline

trigger:
- master

variables:
  group: 'Security'

pool:
  name: 'Default'

steps:

- task: CmdLine@2
  inputs:
    script: |
      curl --request GET --url "https://erasoxavier.atlassian.net/rest/api/3/issue/SCRUM-1/comment" --user "eraso.xavier@gmail.com:%JIRA_TOKEN%" --header "Accept: application/json"
    failOnStderr: false
  env:
    JIRA_TOKEN: $(jiraToken)